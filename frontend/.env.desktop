# Desktop installers use the deployed multi-user backend. Local `vite` dev still
# reaches 127.0.0.1:8848 through vite.config.ts unless this desktop mode is used.
VITE_API_BASE=https://pharos.selab.top/api
